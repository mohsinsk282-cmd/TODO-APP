# ========================================================================
# Database Configuration
# ========================================================================

# Neon PostgreSQL Database Connection
#
# What: Connection string for Neon Serverless PostgreSQL database
# Used by: SQLModel ORM, Alembic migrations, pytest integration tests
#
# Format: postgresql://user:password@host-pooler.region.aws.neon.tech/dbname?sslmode=require
#
# CRITICAL: Use the POOLED connection endpoint (with -pooler suffix)
# - Pooled endpoint: Handles 10,000+ concurrent connections via PgBouncer
# - Direct endpoint: Only supports ~100 connections (will fail under load)
#
# Example: postgresql://neondb_owner:pass@ep-abc-123-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require
#                                                      ^^^^^^^
#                                                      This -pooler suffix is REQUIRED
#
# How to get:
# 1. Sign up at https://neon.tech (free tier available)
# 2. Create project and navigate to "Connection Details"
# 3. Select "Pooled connection" (NOT "Direct connection")
# 4. Copy the full connection string
# 5. Paste it here (replace the example below)
DATABASE_URL=postgresql://user:password@ep-xxx-pooler.us-east-2.aws.neon.tech/todo_db?sslmode=require

# ========================================================================
# Authentication Configuration
# ========================================================================

# Better Auth Secret for JWT Token Verification
#
# What: Shared secret key for signing and verifying JWT tokens
# Used by: Better Auth (frontend), FastAPI JWT middleware (backend)
#
# SECURITY: This secret MUST be:
# - At least 32 characters long
# - Randomly generated (never use a predictable value)
# - Kept secure (never commit to version control)
# - Same value in both frontend and backend .env files
#
# Generate a secure secret (choose ONE method):
#
# Method 1 (OpenSSL - recommended):
#   openssl rand -base64 32
#
# Method 2 (Python):
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# Example output: kJ3mP9qR2sT5vW8xY1zA4bC7dE0fG3hI6jK9lM2nO5pQ
#
# IMPORTANT: Replace the placeholder below with your generated secret
BETTER_AUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32

# ========================================================================
# CORS Configuration
# ========================================================================

# Frontend URL for CORS Whitelist
#
# What: Origin URL of the Next.js frontend application
# Used by: FastAPI CORS middleware to allow cross-origin requests
#
# SECURITY: Only whitelist trusted frontend origins
# - Development: http://localhost:3000 (Next.js dev server default port)
# - Production: https://your-app.vercel.app or your actual domain
#
# Multiple origins can be added in the CORS middleware config
# This variable is the primary/production frontend URL
#
# Examples:
#   Development: http://localhost:3000
#   Production:  https://todo-app.vercel.app
FRONTEND_URL=http://localhost:3000
